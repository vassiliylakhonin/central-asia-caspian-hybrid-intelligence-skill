name: validate

on:
  push:
    branches: ["main"]
  pull_request:

jobs:
  basic-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check required files
        run: |
          test -f README.md
          test -f skills/openclaw/SKILL.md
          test -f skills/codex/SKILL.md
          test -f skills/claude/SKILL.md
          grep -q "## Installation" skills/openclaw/SKILL.md
          grep -q "## Example Prompt" skills/openclaw/SKILL.md
          grep -q "## Installation" skills/claude/SKILL.md
          grep -q "## Example Prompt" skills/claude/SKILL.md
          grep -q "## Installation" skills/codex/SKILL.md
          grep -q "## Example Prompt" skills/codex/SKILL.md
