[project]
name = "regional-compliance-server"
version = "0.1.0"
description = "MCP Server for Autonomous Regional Compliance"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.2.0",
    "pydantic>=2.0.0"
]
